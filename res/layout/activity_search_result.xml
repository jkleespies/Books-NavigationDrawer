<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="right"
    android:orientation="vertical"
    android:background="@drawable/background_image" >

    <!-- Suche EditText und Suchergebnisliste ListView -->
    <TextView
        android:id="@+id/SearchResultActivity_Search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="5dp"
        android:textSize="15sp"
        android:layout_toRightOf="@+id/SearchResultActivity_SearchPhrase"/>
    
    
    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/SearchResultActivity_Search"
        android:divider="#b5b5b5"
        android:dividerHeight="1dp"
        android:listSelector="@drawable/list_selector" >
    </ListView>

    <TextView
        android:id="@+id/SearchResultActivity_SearchPhrase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/SearchResultActivity_Search"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="5dp"
        android:text="@string/searchPhrase" />

</RelativeLayout>